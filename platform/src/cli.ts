// CLI entry for the deterministic *.flow.md parser.
//
// Placeholder — the real implementation lands in Phase 1, Task 4.
// Planned usage: `npm run parse -- <path/to/file.flow.md>` prints the FlowPlan
// as stable-ordered, 2-space-indented JSON.
console.error("parse CLI not yet implemented (Phase 1, Task 4).");
process.exit(1);
